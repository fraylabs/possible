# Review

Review status: **verified local Build Week candidate; external submission pending**.

## Judge test

> Models know how to perform individual tasks. Possible provides the operational knowledge to coordinate those tasks into a verified outcome.

The candidate demonstrates that statement through three distinct layers:

- The typed registry and deterministic compiler make the operational contract inspectable.
- The Still run shows a fresh verifier withholding completion, finding a real integration failure, and passing the repaired outcome.
- The frozen pilot reports Direct at 19/20 and Possible at 20/20 in these runs while preserving every limitation and non-Possible result.

## Acceptance checks

- `/benchmarks` exposes the outcome comparisons, while the raw pilot files preserve the exact result table and limitations.
- The homepage links directly to the three benchmarks and the target user is solo developers and small technical teams.
- The README provides a five-minute judge path, requirements, supported-platform evidence boundary, expected output, and troubleshooting.
- Devpost copy, a sub-three-minute video package, and a claim ledger are ready under `submission/`.
- Build Week history separates observed repository facts from owner attestations.
- `npm run check` passes.

## Required owner actions

1. Perform the outstanding desktop/mobile visual pass when a browser surface is available.
2. Deploy the pushed revision to possible.sh, then verify the benchmark, demo, and raw evidence URLs live.
3. Keep judges on published `0.1.7` or publish and independently verify the matching `0.1.8` candidate; preserve that the measured Possible run used `0.1.6`.
4. Record the final commit, eligible commit range, exact GPT-5.6 evidence, and `/feedback` session ID.
5. Record and upload the final video, then submit the Devpost entry.

The repository does not claim that one pilot proves typical superiority, a speed advantage, cross-platform equivalence, customer validation, deployment, fabrication, or production readiness.

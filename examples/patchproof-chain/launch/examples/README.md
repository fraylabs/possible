# Examples

`passing-draft.json` is a valid PatchProof draft copied from the tested passing fixture. Import it from the product’s **Import JSON** action or compare it with `fixtures/01-passing-evidence.json`.

The example is synthetic and exists to reproduce behavior. It is not customer evidence.

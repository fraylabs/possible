# Independent Working Web App verification

**Verifier:** fresh subagent `/root/chain_product_run/patchproof_fresh_verifier`

**Implementation ownership:** none

**Date:** 2026-07-22

**Status recommendation:** `passed`

## Acceptance results

| Acceptance check | Result | Direct evidence |
|---|---|---|
| Clean install and local run | PASS | A fresh `npm ci` installed the locked dependencies successfully. `README.md` documents Node 20+, `npm ci`, `npm run dev`, and `npm run verify`. |
| Complete browser receipt flow | PASS | Fresh Playwright runs loaded fixtures, generated and inspected receipts, downloaded JSON, detected stale output after edits, regenerated, reloaded the browser, recovered the draft, and reproduced byte-identical JSON. The repository flow passed at 1440×1000 and 390×844. A separate full mobile probe also exercised generation, download, stale/regenerate, refresh recovery, malformed-import recovery, and zero horizontal overflow. |
| Five-state evidence semantics | PASS | All 12 fixtures now assert exact extracted evidence, derived claims, warnings, and limitations. The suite preserves `passed`, `failed`, `skipped`, `unknown`, and `unsupported`; absent evidence cannot pass. A forged imported receipt was recalculated rather than trusted. |
| Material failure recovery | PASS | Malformed JSON left both the active draft and `patchproof:draft:v1` byte-for-byte unchanged and displayed actionable recovery. A hidden nonzero exit remained failed despite later success text. |
| Deterministic fixtures and export | PASS | Each valid fixture generated byte-identical canonical JSON after receipt import/reload. JSON uses stable ordering and a trailing LF; Markdown and JSON are generated from the same receipt value. Explicit limitations survived generation and reload. |
| Responsive behavior | PASS | Desktop and mobile screenshots were inspected. The 390×844 run reported 0 px horizontal overflow, retained the full five-state summary and export action, and produced no console or page errors. |
| Production build and automated proof | PASS | Final `npm run verify` completed 34/34 unit assertions, the Vite production build, and the desktop/mobile browser flow. |
| Scoped safety claims | PASS | `npm audit --json` reported 0 known vulnerabilities. Source/fixture/test scans found no credential, command-execution, unsafe-HTML, upload, analytics, or network path. Browser observation recorded 0 external requests. This supports only the documented local-flow boundary, not a claim that the product is secure. |
| Discovery handoff and authority | PASS | `node .possible/verify-chain-handoff.mjs` revalidated the source receipt, fresh review, pack definition, all transferred evidence, and the archived run hashes after approval. Discovery demand claims remain hypotheses or unknowns. `.possible/chain.json` records `externalAuthority: "none"`; Developer Project Launch remains proposed and unapproved. |
| Repository and path hygiene | PASS | `git diff --check` passed. Referenced evidence resolved to regular files inside the repository. No non-dependency symlinks were present. |

## Commands rerun

```text
npm ci                                  PASS
npm run verify                          PASS — 34 tests, build, browser flow
npm audit --json                        PASS — 0 vulnerabilities
node .possible/verify-chain-handoff.mjs PASS
git diff --check                        PASS
targeted negative receipt probes        PASS
path, symlink, sink, and secret checks  PASS
```

## Failures caught and repaired

Fresh verification did not trust the implementation report and found three completion blockers:

1. The chain verifier crashed after approval because it required a pending transition. It now resolves and validates the single approved inbound handoff for the active stage. The repaired verifier passed against every preserved hash.
2. A conventional `1 failing` line followed by exit code 0 was incorrectly classified as passed. It is now conflicting evidence and remains `unknown`; a regression assertion passes.
3. Fixtures asserted only claim statuses even though the data contract requires exact evidence, warning, and limitation expectations. All 12 fixtures and their tests now cover the complete receipt contract.

The earlier import-dialog, five-state-summary, focus-loss, and data-contract repairs documented in `outcome-room/repair-log.md` were also exercised by the final integrated run.

## Limitations

- Verification covers the documented local Chromium flow, not cross-browser behavior, production deployment, scale, reliability, or comprehensive security.
- Storage-denial, clipboard-denial, and download-initiation failures were inspected in source but not dynamically fault-injected; malformed import was the exercised material recovery path.
- No customer demand, willingness to pay, usability beyond the tested flow, semantic patch correctness, or product-market fit was established. The Discovery assumptions remain explicitly unproven.
- The integration owner must create `outcome-room/product-receipt.json` after this review and bind it to the final immutable workspace revision. This review recommends `passed`; it does not manufacture that revision or receipt.

## Final determination

PatchProof is a genuinely locally runnable Working Web App for the bounded job in the approved brief. Its complete receipt flow, conservative five-state semantics, deterministic fixtures, responsive browser behavior, malformed-import recovery, build, and scoped local-only claims are supported by fresh direct evidence. The Working Web App stage is recommended **`passed`** and eligible for a separately approved next-stage handoff after the integration owner records the final product receipt and immutable revision.

# Fold — verification receipt

## Verified

- The Playable Web Game manifest compiles into three repository-grouped install commands and a complete captain prompt.
- The game uses the packaged `three` runtime and ships through the existing TypeScript and Vite build.
- The core loop exposes ready, playing, paused, and game-over states.
- Pointer, touch-sized pointer events, WASD, arrow keys, pause, mute, score, best score, collision, and restart paths are implemented.
- The WebGL renderer has a visible fallback state.
- The game uses procedural geometry and sound with no external game assets.
- The pack proof is labeled separately from the three preserved clean-room runs.

## Not claimed

- No stable frame-rate guarantee across all devices.
- No broad browser or assistive-technology compatibility claim.
- No player-demand, retention, or production-readiness claim.
- No independent clean-room run of the new pack yet.

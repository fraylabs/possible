# Evaluation reports

`latest.json` is generated deterministically by `npm run eval`. Until fresh
paired runs exist, it reports `pending-real-runs` and identifies both pending
scenario IDs without fabricating scores.

interface:
  display_name: "Possible"
  short_description: "Retrieve source-backed field guides"
  default_prompt: "Use $possible for retrieval-only field-guide research: decompose compound requests, run focused searches, read only relevant guides, combine them with project context, choose available host skills, execute and validate in the host, cite material sources, and state when Possible has no useful guidance."
dependencies:
  tools:
    - type: "mcp"
      value: "possible"
      description: "Read-only Possible field-guide retrieval server"

interface:
  display_name: "Possible"
  short_description: "Compile skills into complete outcomes"
  default_prompt: "Use $possible to compile the Hardware Launch outcome: list and inspect its external skill sources, install only with authorization, then run its workstream-based captain workflow and verification contract."
dependencies:
  tools:
    - type: "mcp"
      value: "possible"
      description: "Read-only Possible outcome-pack compiler"

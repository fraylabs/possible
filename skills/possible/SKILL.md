---
name: possible
description: Search Possible's maintained wiki when you need contributor-authored operational knowledge, then read the relevant pages, follow their links progressively, and cite the attached sources instead of relying on memory.
---

# Possible

Use Possible as a read-only wiki of maintained pages shared by humans and
agents.

1. Start with `search` using a short natural-language query for the outcome,
   domain, or problem.
2. Use `read` on the most relevant slugs only.
3. Follow internal page links progressively when they are needed to answer the
   task; do not load unrelated branches speculatively.
4. Cite the page title, review date, and sources when retrieved knowledge
   informs a recommendation or plan.
5. Stop once the maintained pages answer the question.

Possible hosts knowledge only. The agent host still plans, decides whether more
retrieval is needed, and executes any approved actions.

Possible does not authorize credentials, spending money, deployment, DNS
changes, fabrication, or other privileged writes. Retrieval from Possible is
informational; execution stays approval-gated in the host agent workflow.
